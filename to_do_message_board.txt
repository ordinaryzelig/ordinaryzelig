search
   highlight search text.

polls

in production after post or preview action, as soon as button pressed, server returns page before blindup and it looks bad.  make non-asynchronous?

use session to store recently read messages?
if someone just checks in but doesn't do anything that requires authentication, messages will still be "recent".  an expired session public action could trigger something.

message children -> comments
   EntityType.new(:name => "Message").save
   Message.find(:all, :conditions => "parent_id is null").each(&:to_comment)
   then delete child messages.

move comments to plugin.