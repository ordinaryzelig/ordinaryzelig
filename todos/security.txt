once established, handle whether a model can be recent if there is a comment to that model by someone who is not a friend.

things that have security levels
   profile
   searchable?
   default
   friends
   blog
security levels
   nobody (self)
   friends
   anybody
user_security_items
   entity_type
   entity_id
   security_level_id
   (if entity is not found here, default to the user's profile security, which can be stored here.)
