allow anybody to have their own pool.
  add pool model
    move season columns
      max_num_brackets
      buy-in
    we already have a pool_user model. use pools_users habtm?

make scoring_system model

cache games in CONSTANT?
  caching seasons too.
  make sure to refresh when creating new ones.
  can do this for regions too.

in url, season_id -> season/year
